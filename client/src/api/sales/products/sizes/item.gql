query COMMON_SIZE($id: BigInt!) {
  commonSize(id: $id) {
    commonSpecies {
      id
      speciesName
      commonCategory {
        id
        categoryName
      }
    }
    id
    productSizeId
    productSizesByCommonSizeProductSizeCommonSizeIdAndProductSizeId {
      nodes {
        id
        jvDescription
      }
    }
    sizeDescription
    sizeName
    commonSizeTags {
      nodes {
        commonSizeId
        nodeId
        tagText
      }
    }
  }
}
